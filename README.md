# build-script
Simplified Build command for 3.0 package builds. Works for package building and upgrades in work tree env also.

# Usage
```
Usage: build "<pkg-name> [<pkg-name> ...]"

Examples:
   bash build "tree"
   bash build "tree vim curl"
```
